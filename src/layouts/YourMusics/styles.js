import Colors from '../../config/styles';
import {StyleSheet} from 'react-native';

export const st_background = StyleSheet.create({
  root: {
    flex: 1,
    backgroundColor: Colors.background,
  },
});
