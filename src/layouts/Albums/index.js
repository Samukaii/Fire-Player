import React from 'react';
import {View, Text} from 'react-native';

const Albums = () => (
  <View style={{flex: 1}}>
    <Text style={{justifyContent: 'center', alignItems: 'center'}}>Albums</Text>
  </View>
);
export default Albums;
